<h1>All Books in Database</h1>
<p> Click each book to see it's details</p>

<% @books.each do |book| %>
  <li><%= link_to book.title, "/books/#{book.id}" %></li>
<% end %>

<%= link_to "Add Another Book", "/books/new" %>
